# Worlds

Worlds are the games/maps built for Fumohouse.

1. A world is a module with a defined entry point and a manifest `module.tres`
   that is a `WorldManifest` (which derives from `ModuleManifest`).
1. Worlds can be single or multiplayer.
